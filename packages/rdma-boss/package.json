{
  "name": "@rdma/boss",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Boss agent — final decision-maker. Accepts, sends back for revision, or deploys. Owns accepted, deployed, delivered stages.",
  "main": "./src/agent.ts",
  "types": "./src/agent.ts",
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@rdma/core": "*",
    "tsx": "^4.19.2"
  }
}
