{
  "name": "@rdma/pm",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Product manager agent — drafts PRD, runs clarification rounds, and produces the implementation plan. Owns clarifying, prd_pending_confirmation, and approved_for_dev stages.",
  "main": "./src/agent.ts",
  "types": "./src/agent.ts",
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@rdma/core": "workspace:*",
    "tsx": "^4.19.2"
  }
}