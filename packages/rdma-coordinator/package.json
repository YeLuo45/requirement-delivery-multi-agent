{
  "name": "@rdma/coordinator",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Coordinator agent — registers proposals, captures user intent, dispatches to next agent. Owns the intake stage.",
  "main": "./src/agent.ts",
  "types": "./src/agent.ts",
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@rdma/core": "*",
    "tsx": "^4.19.2"
  }
}