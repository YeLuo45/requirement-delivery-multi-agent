{
  "name": "@rdma/designer",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Designer agent — produces UI/UX spec for UI-bearing requirements. Owns the ideation stage.",
  "main": "./src/agent.ts",
  "types": "./src/agent.ts",
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@rdma/core": "*",
    "tsx": "^4.19.2"
  }
}
