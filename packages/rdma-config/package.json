{
  "name": "@rdma/config",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Per-agent LLM + prompt configuration loader for RDMA. Reads .rdma/agents.yaml and the .rdma/agents/<id>/{soul,user,memory}.md directories.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  }
}
