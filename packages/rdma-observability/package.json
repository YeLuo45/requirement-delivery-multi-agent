{
  "name": "@rdma/observability",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Minimal tracing + metrics primitives for RDMA. Drop-in compatible shape with OTEL Tracer/Span API; no external dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "node --test --import tsx test/*.test.ts"
  },
  "dependencies": {
    "@rdma/core": "*"
  }
}
