# P-20260621-011 PRD: RDMA 1+2+3+4 Control-Plane Integration

## Background

P-20260621-009 added pure decision logic for delivery sandbox planning, collaboration access, tool policy, and cost routing. The next step is to make that logic operational enough for downstream CLI/TUI/Web integration without adding external dependencies.

## Scope

Implement the top four follow-up directions from the last delivery report:

1. Sandbox Executor: apply a file patch inside an isolated sandbox path and produce a reviewable patch bundle.
2. Policy Audit Events: convert tool-policy decisions into publishable audit/event payloads.
3. Cost Metrics Integration: expose budget snapshots as counter metrics.
4. Collaboration UI/TUI Summary: format collaboration decisions as a stable text panel for CLI/TUI/Web reuse.

## Acceptance Criteria

- Tests are written first and the RED failure is captured.
- Incremental coverage for new code is >=95%.
- Full `npm test` pass rate is 100%.
- `npm run coverage` passes the source coverage gate.
- `npm run verify:readme` reports 0 failed commands.
- `npm run check` and `npm run build` pass after README verification side effects are cleaned.
